#include<iostream>
using namespace std;

int Pow(int m, int n)
{
    if(n==0)
    return 1;
    else
    return Pow(m,n-1)*m;
}

int	main()
{
    int a;
    a=Pow(2,5);
    cout << a<<endl;


    
    return 0;
}
